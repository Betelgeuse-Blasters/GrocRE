export const getAllPosts = (page, count) => {
  return {
    method: "GET",
    url: `THEURLOFTHE PLACE GETTING CALLED`,
    params: {
      page: page,
      count: count,
    },
    headers: {},
  };
};
